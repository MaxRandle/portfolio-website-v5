import { Container } from "@ui/Container";
import { Section } from "@ui/Section";

export const AISection: React.FC = () => {
  return (
    <Section spacing="lg">
      <Container>I'm good at using agentic tools</Container>
    </Section>
  );
};
