This folder is for utility functions, typically one util export per file unless the functions are closely related.

Most utility functions are paired with a corresponding test file:

- `[utility-function-name].ts`
- `[utility-function-name].test.ts`
